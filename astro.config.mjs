import { defineConfig } from 'astro/config';

// IMPORTANT: change this to your real domain once it's live.
// This powers canonical URLs and social-share (OpenGraph) links.
export default defineConfig({
  site: 'https://www.dylangillette.com',
});
